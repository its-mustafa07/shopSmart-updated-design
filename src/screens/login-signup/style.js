const {StyleSheet} = require('react-native');

export const styles = StyleSheet.create({
  container: {
    // flex: 1,
    // justifyContent: 'center',
    // alignItems: 'center',
    width: '100%',
    flexDirection: 'row',
    overflow: 'hidden',
  },
  animationContainer: {
    flexDirection: 'row',
    width: '50%',
    height: '100%',
  },
});
